// This file would typically import and configure fonts
// For now, we're just creating a placeholder since we don't have direct access to font files

export const fonts = {
  sigmar: "Sigmar, -apple-system, Roboto, Helvetica, sans-serif",
  sans: "-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Oxygen, Ubuntu, Cantarell, Fira Sans, Droid Sans, Helvetica Neue, sans-serif",
};

// In a real implementation, you would import fonts like this:
// import { Sigmar } from 'next/font/google'
// export const sigmar = Sigmar({ subsets: ['latin'], weight: ['400'] })
